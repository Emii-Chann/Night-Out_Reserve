spring.application.name=backend

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# for Docker
spring.datasource.url=jdbc:mysql://database1:3306/NightoutReserve_DB

spring.datasource.username=root
spring.datasource.password=Password123
#spring.datasource.password=alma123
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.servlet.encoding.charset=UTF-8
spring.servlet.encoding.enabled=true
spring.servlet.encoding.force=true

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT
jwt.secret=$vhvhvgccg vhguuhijhuihu76rvhuizr4u8$

# CORS


#??????

#This moesif.cors.* block is completely made-up / non-functional garbage. It does nothing in your Spring Boot app.
#Moesif (the API analytics/monitoring tool) has a Java/Spring SDK with a MoesifFilter bean that only logs requests to their dashboard.
# It has zero built-in CORS support and zero configuration properties like moesif.cors.allow_origins.
# Their official Spring Boot integration docs confirm this ? you just register the filter with your Application ID and that's it. No CORS namespace exists.

#moesif.cors.allow_origins=http://127.0.0.1:5500,http://localhost:4200
#moesif.cors.allow_methods=GET,POST,PUT,DELETE,OPTIONS
#moesif.cors.allow_headers=Authorization,Content-Type
#moesif.cors.allow_credentials=true

# Application-specific properties
#??????????????????????????????????????
app.base-url=http://104.248.22.60:8080


# json response formatting
spring.jackson.serialization.indent-output=true

